{
  "name": "magnetic-lacquer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Physics-driven 3D sandbox for previewing magnetic (cat-eye / velvet) nail polish finishes",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "sync:tests": "node scripts/sync-test-count.mjs",
    "check:tests": "node scripts/sync-test-count.mjs --check"
  },
  "dependencies": {
    "lil-gui": "^0.20.0",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
